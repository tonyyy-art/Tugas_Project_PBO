import com.login.Login;

public class LoginSystem {
    public static void main(String[] args) {
        
        // Create an instance of the Login class
        Login login = new Login();

        // Call the login method to start the login process
        login.LoginMenu();
        
    }
}
